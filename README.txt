Satrass Fix Bundle
------------------
1) Place these files at your repo root (next to package.json).
2) Run:   bash apply-fixes.sh
3) Commit & push.

This adds Tailwind/PostCSS devDeps and config, adds lib/withCsrf.js,
and fixes API imports and a typo in handlers.
